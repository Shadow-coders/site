// const guilds:any = []
// const client = { guilds: { cache: new Map<any,any>() } }
// const user = { id: '' }
//  guilds.filter((g:any) => {
//     const guild = client.guilds.cache.get(g.id);
//     return g ? true : false;
//      }).map((g:any) => client.guilds.cache.get(g.id)).forEach((g:any) => {  
//     if(!g.members.cache.get(user.id).permissions.has('MANAGE_SERVER')) { 

         
//     return;
//     }  
//      if(g.icon) { g.iconURL() }
//     if(!g.icon) { g.nameAcronym  }
// })
       